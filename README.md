# Nanji Japan — React Native (Expo) + TypeScript + i18n (Fixed)

- ✅ 修复 Web 端打包缺失 `react-native-web` 的问题
- 多语言：中文 / English / 日本語（Header 右上角切换）

## 运行
```bash
npm i
npm run web   # 或 npm run start 后选择 web
```
如仍遇到缓存问题：
```bash
rm -rf node_modules
npm i
expo start --clear
```
